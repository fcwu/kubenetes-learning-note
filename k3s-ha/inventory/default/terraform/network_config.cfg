version: 2
ethernets:
  ens3:
    addresses:
      - ${ipv4_address}/${ipv4_netmask}
    gateway4: ${gateway4}
    nameservers:
      addresses: ${nameservers}
  ens4:
    addresses:
      - ${ipv4_drbd_address}/${ipv4_drbd_netmask}
